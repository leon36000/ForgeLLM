# NVIDIA Agent Skills Relevant to Later ForgeLLM Phases

The current environment exposes NVIDIA-oriented skills that may help after ForgeLLM reaches deployment and distributed-serving work:

- Dynamo router starter for validated routing modes and endpoint smoke tests;
- Dynamo interconnect check for NIXL/UCX/NCCL readiness on disaggregated deployments;
- AI-Q deployment/research skills for their specific blueprint workflows.

They are not substitutes for ForgeLLM's architecture or benchmark protocol. Use them only when the task matches their scope, read the current skill first, and do not install or change agent capabilities without owner approval.
